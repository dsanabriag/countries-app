import ViewList from '@/components/viewList';

export const metadata = {
  title: 'Países - Countries App',
  description: 'Explora información detallada de países alrededor del mundo',
};

export default function CountriesPage() {
  return <ViewList />;
}
